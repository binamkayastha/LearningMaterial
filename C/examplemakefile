all: program.c
	gcc -g -Wall -o programname program.c

clean:
	rm programname

#More info:
target: dependency1 dependency2
	<tab> command

for example:

program: program.o mylib.o
	gcc -o program program.o mylib.o
